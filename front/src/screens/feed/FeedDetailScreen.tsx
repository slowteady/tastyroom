import React from 'react';
import {StyleSheet, View} from 'react-native';

interface FeedDetailScreenProps {
  //
}

const FeedDetailScreen = ({}: FeedDetailScreenProps) => {
  return <View></View>;
};

const styles = StyleSheet.create({});

export default FeedDetailScreen;
